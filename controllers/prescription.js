const Prescription = require('../models/Prescription');


module.exports = {

    // async create(req, res) {
    //     try {
    //       const user = req.user

    //         let id=req.params.id;
    //      let precription=await Prescription.service(id,req.body)

            
    //     } catch (err) {
    //         console.error(err);
    //         res.status(400).json({ error: err.message });
    //     }

    // }

}